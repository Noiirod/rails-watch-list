<h1>Add a film</h1>
<%= simple_form_for (@bookmark, as: :bookmark, method: :post, url: list_bookmarks_path) do |f| %>
  <%= f.input :comment %>
  <%= f.association :movie %>
  <% f.input :list %>
  <%= f.submit "Add", class: "btn btn-primary" %>
<% end %>
